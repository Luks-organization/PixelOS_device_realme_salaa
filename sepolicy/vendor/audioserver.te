allow audioserver unlabeled:dir search;
allow audioserver unlabeled:file { read write open getattr };
allow hal_audio_default hal_audio_default:process { execmem };
allow audioserver vendor_default_prop:file { read write open getattr map };

get_prop(audioserver, vendor_mtk_audio_prop)
get_prop(audioserver, vendor_audioserver_prop)
get_prop(cameraserver, vendor_oplus_prop)
